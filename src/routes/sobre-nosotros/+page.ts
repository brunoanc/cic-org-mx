import type { PageLoad } from "./$types";

export const load: PageLoad = () => {
    return {
        title: "Sobre nosotros - CIC",
        description: "Conoce más sobre nosotros, nuestra misión, visión y valores."
    };
};
