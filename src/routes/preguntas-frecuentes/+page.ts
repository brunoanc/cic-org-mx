import type { PageLoad } from "./$types";

export const load: PageLoad = () => {
    return {
        title: "Preguntas frecuentes - CIC",
        description: "Resuelve tus dudas sobre la organización."
    };
};
