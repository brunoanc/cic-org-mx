import type { PageLoad } from "./$types";

export const load: PageLoad = () => {
    return {
        title: "Eventos - CIC",
        description: "Descubre los eventos organizados por CIC."
    };
};
