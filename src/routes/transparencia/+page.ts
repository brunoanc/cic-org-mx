import type { PageLoad } from "./$types";

export const load: PageLoad = () => {
    return {
        title: "Transparencia - CIC",
        description: "Conoce como utilizamos nuestros fondos."
    };
};
